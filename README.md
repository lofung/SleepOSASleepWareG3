# SleepOSASleepWareG3
auto annotation scripts for G3

requires Stanford sleep center annotation files to run, which is a standardized txt format.

After running the script, the txt would be transformed as XML codes to be copied into the .rml files for Sleepware G3 and be read by the software.
